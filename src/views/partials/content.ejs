<li>
    <h4 class="display-4"><%= post.titulo %></h4>
    <span class="lead" ><%= post.contenido %></span>
    <img src="<%= post.link_img %>" >
    <a href="/posts/eliminar-post/<%= post.id %>" class="btn btn-danger">eliminar</a>
</li>